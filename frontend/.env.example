# Base URL for the backend API
# In development, Vite proxies /api → backend:8000 (see vite.config.ts)
# In production, set this to the public API URL if not using the nginx proxy
VITE_API_BASE_URL=/api/v1
